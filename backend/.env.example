DJANGO_SECRET_KEY=change-me-in-dev
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Overridden by docker-compose.yml when running via `docker compose up`.
# Set this yourself only when running the backend outside Docker.
# DATABASE_URL=postgres://bienvenue:bienvenue@localhost:15432/bienvenue
